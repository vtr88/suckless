#!/bin/sh

cache="${XDG_CACHE_HOME:-$HOME/.cache}/slstatus/result.txt"

# Mantem o ultimo resultado valido quando a consulta de rede falhar.
if [ -s "$cache" ]; then
	sed 's/🇧🇷/^c#009739^▰^c#fedd00^◆^c#3e4095^●^c#8ec07c^/g' "$cache"
fi
